# {{PROJECT_NAME}} - Agent Entry

Read [START_HERE.md](START_HERE.md) first. It is the shared quick-start card
for every AI working on this repo.

This project inherits Global RTK (`~/.rtk/RTK.md`).

Before non-trivial work:

- Read `.rtk/project.md`.
- Read only the relevant files in `.rtk/memory/`.
- Project rules override global rules only when they are stricter.
- Default to Plan before Apply.
- Use minimal context.
- No secrets, no full repo scan, no destructive commands.
- Do not deploy or publish unless {{OWNER}} explicitly approves.
