# Mistakes To Avoid

Every line here should be something that actually went wrong once. Do not
pre-populate it with hypotheticals - the file loses its authority.

- Do not ship without explicit approval from {{OWNER}}.
- Do not stage unrelated dirty files with the current task.
- Do not rewrite many files at once unless explicitly approved.
- Do not assume a deploy is live the moment the command returns.

<!-- TODO: append yours, newest at the bottom, one line each. -->
